import { LinesManagerPipe } from './lines-manager.pipe';

describe('LinesManagerPipe', () => {
  it('create an instance', () => {
    const pipe = new LinesManagerPipe();
    expect(pipe).toBeTruthy();
  });
});
